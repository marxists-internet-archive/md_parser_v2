export const updateMeta = state => {
  return {
    type: "UPDATE_META",
    payload: state
  };
};

export const updateDate = state => {
  return {
    type: "UPDATE_DATE",
    payload: state
  };
};
