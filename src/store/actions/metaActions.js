export const updateMeta = state => {
  return {
    type: "UPDATE_META",
    payload: state
  };
};

export const uploadMeta = state => {
  return {
    type: "UPLOAD_META",
    payload: state
  };
};
